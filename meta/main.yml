---
dependencies: []
galaxy_info:
  role_name: ansible_role_users
  namespace: geant
  description: Configure multiple users accounts, including ssh_config, SSH keys (public/private), multiple allowed commands in authorized_keys
  author: Dick Visser
  company: GÉANT
  license: MIT
  min_ansible_version: "9"
  platforms:
    - name: EL
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: FreeBSD
      versions:
        - all
  galaxy_tags:
    - system
    - users
    - accounts
